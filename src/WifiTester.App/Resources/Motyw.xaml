<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <BooleanToVisibilityConverter x:Key="BoolToVis"/>

  <!-- Paleta TMA (RAL + zieleń TMA) -->
  <SolidColorBrush x:Key="HdrBg"   Color="#232A2D"/>
  <SolidColorBrush x:Key="StepBg"  Color="#3F484A"/>
  <SolidColorBrush x:Key="Bg"      Color="#D4D1CA"/>
  <SolidColorBrush x:Key="TmaGrn"  Color="#37A829"/>
  <SolidColorBrush x:Key="Card"    Color="#E8E5DE"/>
  <SolidColorBrush x:Key="HdrTxt"  Color="#E0DDD6"/>
  <SolidColorBrush x:Key="Muted"   Color="#6B7070"/>
  <SolidColorBrush x:Key="Err"     Color="#c0392b"/>
  <SolidColorBrush x:Key="Warn"    Color="#d35400"/>
  <SolidColorBrush x:Key="UpdBarBg" Color="#fef3cd"/>
  <SolidColorBrush x:Key="UpdBtn"   Color="#ffc107"/>

  <Style x:Key="PrzyciskGlowny" TargetType="Button">
    <Setter Property="Background" Value="{StaticResource TmaGrn}"/>
    <Setter Property="Foreground" Value="White"/>
    <Setter Property="Padding" Value="14,6"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}" CornerRadius="4"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="KartaTekst" TargetType="TextBlock">
    <Setter Property="Foreground" Value="#232A2D"/>
  </Style>
</ResourceDictionary>
